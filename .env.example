# HuggingFace token (required for gated models)
HF_TOKEN=your_huggingface_token_here

# Mock mode — set true to run without any model inference (demo/dev)
MOCK_MODE=true

# Optional: use HF Inference API instead of local model loading (faster cold start)
USE_INFERENCE_API=false
